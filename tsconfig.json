{
  "compilerOptions": {
    // "strict": true, // 启用严格检查
    "module": "commonjs",
    // 编译出的代码采用的模块规范
    "target": "es2016",
    // 编译出的代码采用 ES 的哪个版本
    "sourceMap": true
    // 输出 SourceMap 方便调试
  },
  "include": ["./src/*.d.ts"],
  "exclude": [
    // 不编译这些目录里的文件
    "node_modules"
  ]
}
